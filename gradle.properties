# Enable AndroidX support for compiling modern dependencies (like Jetpack Compose)
android.useAndroidX=true

# Optimize Gradle daemon memory consumption
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
